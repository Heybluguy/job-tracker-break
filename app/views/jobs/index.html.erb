All Jobs

<ul>
<% @jobs.each do |job| %>
  <li class="job">
    <%= job.title %> at <%= job.company.name %>
    <%= job.city %>
    <%= link_to "Delete", job_path(job), method: "delete" %>
  </li>
<% end %>
</ul>

